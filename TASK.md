The workspace contains a small Python package geom/ with one function area_of_circle we want to rename to circle_area everywhere. After every rename, log a line to audit.csv with the format student_id,timestamp,file_path,line_number. The student ID comes from the lookup_student_record MCP server — your name is whatever the server returns for S001. Do all of this through the agent, end-to-end.